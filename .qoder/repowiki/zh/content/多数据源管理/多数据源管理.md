# 多数据源管理

<cite>
**本文档引用的文件**
- [README.md](file://README.md)
- [gormplus_datasource.go](file://gormplus_datasource.go)
- [manager.go](file://datasource/manager.go)
- [gormplus.go](file://gormplus.go)
- [ctx.go](file://plugin/ctx.go)
- [tenant.go](file://plugin/tenant.go)
</cite>

## 更新摘要
**所做更改**
- 更新以反映 README.md 中多数据源管理文档的简化：移除了详细的高级特性和实现说明
- 保留基本使用示例和核心概念介绍
- 简化架构说明，专注于实际使用场景
- 移除深入的技术实现细节和性能优化建议

## 目录
1. [简介](#简介)
2. [快速开始](#快速开始)
3. [核心概念](#核心概念)
4. [使用示例](#使用示例)
5. [中间件集成](#中间件集成)
6. [数据源配置](#数据源配置)
7. [常见场景](#常见场景)
8. [故障排查](#故障排查)

## 简介
多数据源管理模块提供了一套完整的数据库连接管理解决方案，支持一主多从架构、读写分离、上下文自动切换等功能。模块采用懒连接设计，首次使用时才建立数据库连接，避免启动阻塞。

## 快速开始
以下是最简单的使用方式，展示如何快速集成多数据源管理：

```go
import (
    "gorm.io/driver/mysql"
    "github.com/kuangshp/gorm-plus"
)

func main() {
    // 注册多数据源
    gormplus.DS.Register("default", gormplus.DataSourceGroupConfig{
        Master: gormplus.DataSourceNodeConfig{
            Dialector: mysql.Open("root:pwd@tcp(master:3306)/mydb?charset=utf8mb4&parseTime=True"),
            Pool:      gormplus.DataSourcePoolConfig{MaxOpen: 50, MaxIdle: 10},
        },
        Slaves: []gormplus.DataSourceNodeConfig{
            {Dialector: mysql.Open("root:pwd@tcp(slave:3306)/mydb?charset=utf8mb4&parseTime=True")},
        },
    })
    
    // 在中间件中设置读写标记
    func DSMiddleware(name string) gin.HandlerFunc {
        return func(c *gin.Context) {
            ctx := gormplus.DSWithName(c.Request.Context(), name)
            if c.Request.Method == http.MethodGet {
                ctx = gormplus.DSWithRead(ctx)  // GET → 从库
            } else {
                ctx = gormplus.DSWithWrite(ctx) // 其他 → 主库
            }
            c.Request = c.Request.WithContext(ctx)
            c.Next()
        }
    }
    
    // 在仓储层获取数据库连接
    func (r *OrderRepo) List(ctx context.Context) ([]*Order, error) {
        db, err := gormplus.DS.Auto(ctx)
        if err != nil { return nil, err }
        var list []*Order
        return list, db.WithContext(ctx).Find(&list).Error
    }
}
```

## 核心概念
- **数据源组**：包含一个主库和多个从库的配置集合
- **懒连接**：首次使用时才建立数据库连接
- **读写分离**：通过上下文标记自动选择主库或从库
- **上下文工具**：提供数据源名和读写标记的设置与读取

## 使用示例

### 基本数据源注册
```go
// MySQL 数据源
gormplus.DS.Register("default", gormplus.DataSourceGroupConfig{
    Master: gormplus.DataSourceNodeConfig{
        Dialector: mysql.Open("root:pwd@tcp(master:3306)/mydb?charset=utf8mb4&parseTime=True"),
        Pool:      gormplus.DataSourcePoolConfig{MaxOpen: 50, MaxIdle: 10},
    },
    Slaves: []gormplus.DataSourceNodeConfig{
        {Dialector: mysql.Open("root:pwd@tcp(slave1:3306)/mydb?charset=utf8mb4&parseTime=True")},
        {Dialector: mysql.Open("root:pwd@tcp(slave2:3306)/mydb?charset=utf8mb4&parseTime=True")},
    },
})

// PostgreSQL 数据源
gormplus.DS.Register("pg", gormplus.DataSourceGroupConfig{
    Master: gormplus.DataSourceNodeConfig{
        Dialector: postgres.Open("host=localhost user=root password=pwd dbname=mydb port=5432 sslmode=disable"),
    },
})
```

### 仓储层使用
```go
// 推荐：使用 Auto 自动读取上下文决定数据源和读写
func (r *OrderRepo) List(ctx context.Context) ([]*Order, error) {
    db, err := gormplus.DS.Auto(ctx)
    if err != nil { return nil, err }
    var list []*Order
    return list, db.WithContext(ctx).Find(&list).Error
}

// 显式指定
db, err := gormplus.DS.Write("default")              // 主库
db, err := gormplus.DS.Read("default")               // 从库
db, err := gormplus.DS.WriteCtx(ctx, "analytics")    // 指定数据源主库
```

## 中间件集成
推荐在中间件中设置数据源名和读写标记：

```go
func DSMiddleware(name string) gin.HandlerFunc {
    return func(c *gin.Context) {
        ctx := gormplus.DSWithName(c.Request.Context(), name)
        if c.Request.Method == http.MethodGet {
            ctx = gormplus.DSWithRead(ctx)  // GET → 从库
        } else {
            ctx = gormplus.DSWithWrite(ctx) // 其他 → 主库
        }
        c.Request = c.Request.WithContext(ctx)
        c.Next()
    }
}
```

## 数据源配置
- **Dialector**：数据库驱动（推荐方式，支持任意 gorm 驱动）
- **DSN**：连接字符串（向后兼容，已弃用）
- **Pool**：连接池配置（MaxOpen、MaxIdle、MaxLifetime、MaxIdleTime）
- **GormConfig**：自定义 gorm 配置

## 常见场景
- **单数据源**：只配置主库，无从库
- **多数据源**：不同业务使用不同的数据源组
- **混合驱动**：同一应用中使用多种数据库类型
- **读写分离**：GET 请求走从库，其他请求走主库

## 故障排查
- **未找到数据源名**：检查中间件是否正确调用 DSWithName
- **连接失败**：检查 Dialector/DSN 配置与驱动导入
- **读写标记冲突**：确认中间件是否正确区分 GET 与其他方法
- **上下文解析问题**：确保插件能从 Request.Context 读取数据

**章节来源**
- [README.md:139-215](file://README.md#L139-L215)
- [gormplus_datasource.go:35-94](file://gormplus_datasource.go#L35-L94)
- [manager.go:26-81](file://datasource/manager.go#L26-L81)
- [manager.go:288-332](file://datasource/manager.go#L288-L332)
- [manager.go:539-579](file://datasource/manager.go#L539-L579)